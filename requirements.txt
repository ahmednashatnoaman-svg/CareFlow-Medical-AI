# Runtime dependencies for the Vercel Python function (api/index.py).
#
# Keep in sync with [project.dependencies] in pyproject.toml, which is the source of
# truth; this file exists because Vercel's Python builder reads requirements.txt.
#
# Deliberately EXCLUDED (see pyproject [project.optional-dependencies].local-embeddings):
#   torch, transformers, sentence-transformers, fastembed
# These exceed the 250MB serverless function limit. In production the embedder is the
# hosted Modal endpoint (EMBEDDER_ENDPOINT_URL); imports of the local stack are guarded
# and simply advance to the next provider when absent.

# --- web framework -------------------------------------------------------------
fastapi>=0.110.0
starlette>=0.36.0
uvicorn[standard]>=0.28.0
python-multipart>=0.0.9

# --- config & validation -------------------------------------------------------
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# --- persistence ---------------------------------------------------------------
sqlalchemy>=2.0.28
aiosqlite>=0.20.0
greenlet>=3.0.0
redis>=5.0.0

# --- retrieval -----------------------------------------------------------------
qdrant-client>=1.8.0
networkx>=3.2.0
pandas>=2.2.0

# --- LLM providers -------------------------------------------------------------
google-genai>=1.0.0
groq>=0.9.0
openai>=1.0.0
langchain-core>=0.1.0
langchain>=0.1.0
langchain-openai>=0.1.0
langchain-groq>=0.1.0
langchain-google-genai>=1.0.0
langchain-community>=0.0.25
langgraph>=0.0.26

# --- misc ----------------------------------------------------------------------
httpx>=0.27.0
beautifulsoup4>=4.12.0
nest-asyncio>=1.6.0
